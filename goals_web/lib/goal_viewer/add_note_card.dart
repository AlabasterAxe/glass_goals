import 'package:flutter/material.dart';

class AddNoteCard extends StatelessWidget {
  const AddNoteCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text("Blah"));
  }
}
