name: goals_core
description: Core Library that would be shared by any app for displaying goals
version: 1.0.0

publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  hive: ^2.2.3
  hlc: ^1.0.3
  rxdart: ^0.28.0
  uuid: ^4.1.0
  cloud_firestore: ^5.4.2
  firebase_auth: ^5.3.0
  goals_types: 
    path:
      ../goals_types
  collection: ^1.17.2
  intl: ^0.19.0
  logging: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  lints: ^4.0.0
  test: ^1.16.0
